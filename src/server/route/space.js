export default {
    // 空间数据（格式化）
    spaceFormat: '/space/all/format',
    // 是否有园区
    haveZone: '/zone/exist',
    // 关联空间
    relateSpaces: '/spaces/{0}/{1}',
    // 角色关联的空间
    roleSpaces: '/role/{0}/spaces'
}
